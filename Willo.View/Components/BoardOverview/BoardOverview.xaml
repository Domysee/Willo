<UserControl
    x:Class="Willo.View.Components.BoardOverview.BoardOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Willo.View.Components.BoardOverview"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:logic="using:Willo.Logic.BoardOverview"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Loaded="UserControl_Loaded">
    <StackPanel Orientation="Vertical">
        <TextBlock HorizontalAlignment="Center" FontSize="20px">Boards</TextBlock>
        <ListView ItemsSource="{Binding Path=Boards}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal"></ItemsWrapGrid>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="logic:OverviewBoard">
                    <Border Height="114px" Width="300px" Background="#0079BF" CornerRadius="3" Margin="5 5 5 5">
                        <TextBlock Text="{x:Bind Path=Name}" Foreground="White" FontSize="20px" FontWeight="Bold" Margin="8"></TextBlock>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
